const BASE_URL = 'https://snazzy-teeth-production.up.railway.app';
const PRODUCT = '/api/product'

export const API_URLS = {
    TOP_PRODUCTS: `${BASE_URL}${PRODUCT}/top`,
    ALL_PRODUCTS: `${BASE_URL}${PRODUCT}`
}