import { PercentageDiscountPipe } from './percentage-discount.pipe';

describe('PercentageDiscountPipe', () => {
  it('create an instance', () => {
    const pipe = new PercentageDiscountPipe();
    expect(pipe).toBeTruthy();
  });
});
